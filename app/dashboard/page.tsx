export default function DashboardPage() {
  return (
    <main className="min-h-screen bg-charcoal text-text-primary">
      <div className="flex h-screen flex-col items-center justify-center">
        <h1 className="text-3xl font-semibold text-emerald-glow">Panchayat Dashboard</h1>
        <p className="mt-2 text-text-muted">Capacity, passes &amp; hazard monitoring</p>
      </div>
    </main>
  );
}
